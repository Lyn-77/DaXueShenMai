# DaXueShenMai 仓库 Agent 执行规范

最后更新：2026-08-25

本文件是仓库级活规范，约束所有在本仓库中工作的 Agent。它不是历史记录，也不是可选参考。每次开工、恢复上下文或接手任务时，都必须依照本文件执行。

## 1. 规范体系与权威关系

本仓库的规范文件分工如下：

| 文件 | 规范职责 | 是否具有约束力 |
|---|---|---|
| 用户最新明确指令 | 决定当前目标、授权范围和需求变更 | 是，最高优先级 |
| `AGENTS.md` | 规定 Agent 如何开工、协作、修改、验证、交付和维护活文档 | 是 |
| `docs/SPEC.md` 与 `docs/spec/*.md` | 规定产品范围、运行行为、输出契约、可靠性、测试和安装要求 | 是 |
| `docs/project/lessons.md` | 记录已发生错误及永久防止规则 | 是，必须在开工时读取 |
| `docs/project/progress.md` | 记录当前事实状态，不负责创造新需求 | 是，作为进度事实来源 |

执行规则：

1. 用户最新明确指令优先于仓库内旧记录。
2. `AGENTS.md` 与 `docs/SPEC.md` 同为规范来源，但职责不同：前者管 Agent 的工作方式，后者管产品应当是什么。
3. 两份规范出现冲突时，禁止静默选择其中一份继续；必须指出冲突位置和影响，交由用户裁决。
4. 用户裁决形成长期规则后，必须同步更新受影响的 `AGENTS.md`、`docs/SPEC.md`、追踪矩阵、`docs/project/lessons.md` 或 `docs/project/progress.md`，不得只修改其中一处。
5. `docs/project/progress.md` 中的状态不得覆盖规范要求；状态与规范不一致时，以规范为准并修正状态记录。

## 2. 每次开工的强制顺序

任何读取、规划、编辑或测试之前，按以下顺序执行：

1. 完整阅读 `docs/project/lessons.md`，不得只看最近一条。
2. 完整阅读本 `AGENTS.md`。
3. 阅读 `docs/SPEC.md`；再按任务范围读取其中指向的全部相关分层规范。
4. 阅读 `docs/project/progress.md`，确认 `[Done]`、`[Doing]`、`[Next]`、`[Bugs]`。
5. 检查当前工作目录、仓库文件清单和 Git 状态，识别用户已有修改与未跟踪文件。
6. 验证实际仓库路径为 `/Users/lyn-77/CODE/DaXueShenMai`；不得继续使用旧路径 `/Users/lyn-77/CODE/KaoYan`。
7. 确认当前阶段和授权门槛，再决定允许执行的动作。

如果上下文经过压缩或由其他 Agent 接手，则在完成第 1、2 步后读取 `docs/spec/99-context-capsule.md`，然后继续执行第 3—7 步。胶囊只用于恢复背景，不替代任何原始规范。

## 3. 固定项目事实

- 仓库：`DaXueShenMai`。
- 远程地址：`https://github.com/Lyn-77/DaXueShenMai.git`。
- 目标结构：一个仓库包含 `dxsm-math`、`dxsm-english`、`dxsm-408` 三个独立 Skill。
- 当前版本：v1 只建设 `dxsm-math`。
- `dxsm-math` 覆盖数学一、数学二、数学三官方考研数学范围的并集。
- 架构：完全由承载 Skill 的 Agent 执行；不使用 API Key，不保存密钥，不直接调用云端模型接口。
- 依赖：零强制第三方依赖；不自动安装缺失工具。
- 许可证：MIT。
- 安装：仓库只提供通用安装说明，不自动安装到用户环境。
- 发布：未经用户明确授权，不提交、不推送、不发布。
- 本地真题 PDF 只用于验收，不复制、不提交、不打包进仓库。
- `.obsidian/` 是本地编辑器状态，必须由根 `.gitignore` 排除，不得提交。

以上事实发生变化时，必须先获得用户明确决定，再同步修改全部相关规范。

## 4. 分层职责

### 4.1 需求与规范职责

输入：

- 用户的新需求、纠正或范围变更；
- 现有 `AGENTS.md`、`docs/SPEC.md`、`docs/project/lessons.md` 和 `docs/project/progress.md`。

必须执行：

1. 将新决定与已有规范逐条比对。
2. 识别冲突、遗漏、隐含假设和受影响文件。
3. 对会改变产品行为或交付范围的未知项，一次只问一个问题。
4. 抽象问题必须配同一具体场景的直观示例。
5. 达到至少 95% 的理解置信度后，先给出实现方案。
6. 为新产品要求分配唯一规范编号，并更新追踪矩阵。

完成条件：

- 需求有明确输入、输出、边界、失败处理和验收方式；
- 没有已知且未裁决的冲突；
- 用户明确批准实现方案后，才可越过实施门。

禁止：

- 把推测写成已确认需求；
- 一次堆叠多个需要用户决策的问题；
- 在实施门未打开时创建代码、Skill 脚手架或测试实现。

### 4.2 实现职责

生效条件：用户明确批准实现方案。

必须执行：

1. 只实现已批准且能追踪到规范编号的行为。
2. 使用开放 Agent Skills 目录格式；Skill 目录名与 frontmatter `name` 必须一致。
3. 优先复用仓库已有资源，保持渐进式披露，避免把全部细则堆进主 `SKILL.md`。
4. 保持 Agent-only、零 API Key、零强制依赖。
5. 修改范围保持最小，保留用户已有内容和无关改动。
6. 实现过程中发现规范不可执行或相互矛盾时立即停止相关部分，回到规范职责处理。

完成条件：

- 实现与全部适用 MUST 要求一致；
- 结构校验、行为测试和阻断项测试通过；
- 文档、进度与实际文件状态一致。

禁止：

- 扩大到英语或 408 的实现在 v1 中占位冒充完成；
- 添加未经批准的联网服务、密钥、遥测或云端调用；
- 为方便实现而静默削弱规范。

### 4.3 数学可靠性与测试职责

必须执行：

1. 所有数学判断必须能追溯到题干、定义、定理、推导、反例或独立验证。
2. 复杂计算在本地工具可用时复核；采信前检查输入、假设、实现和精度。
3. 推导与工具、答案册或不同方法冲突时，回退到首次分歧处同时审查，不隐藏冲突。
4. 测试至少包含：
   - 24 个公开原创合成用例；
   - 36 道固定真题回归，数一、数二、数三各 12 道；
   - 12 道未见题，数一、数二、数三各 4 道。
5. 最终答案、关键推导、定理条件、无依据生成、输入澄清、错解定位、越界控制和冲突回退等阻断项必须 100% 通过。
6. 修复后依次重跑失败用例、同类用例、固定回归；必要时重跑未见题。

禁止：

- 猜测标准答案、评分、阅卷得分点或题目来源；
- 把答案册当作无需复核的绝对真理；
- 为运行测试而复制六份本地 PDF 到仓库；
- 提交 `.obsidian/` 或其他纯本地编辑器状态；
- 声称未实际运行的测试已经通过。

### 4.4 文档、安装与发布职责

必须执行：

1. 项目文档统一维护在 `docs/`；根目录只保留自动发现或仓库入口所需的 `AGENTS.md`、未来的中文 `README.md` 和 `LICENSE`。
2. README 同时说明人类手动安装和把仓库 URL 交给 Agent 安装两种流程。
3. 安装路径只使用通用 `~/.agents/skills/` 表达，不承诺未验证客户端行为完全一致。
4. LICENSE 使用 MIT。
5. 交付前说明实际修改文件、验证结果、未完成事项以及是否提交、安装或推送。

禁止：

- 在 Skill 子目录添加重复 README；
- 自动安装 Skill 或第三方依赖；
- 未经授权执行 Git 提交、远程推送或发布；
- 宣称所有 Agent 客户端都已兼容，除非有实际验证证据。

### 4.5 活文档维护职责

- `docs/project/lessons.md`：每次用户纠正或 Agent 自我纠错后立即追加“现象 → 原因 → 防止规则”。
- `docs/project/progress.md`：阶段工作完成后同步真实状态；未完成事项不得写入 `[Done]`。
- `AGENTS.md`：纠正形成长期执行规则时同步更新。
- `docs/SPEC.md` 与分层规范：产品行为、范围或验收标准改变时同步更新。
- `docs/spec/09-traceability.md`：任何产品要求增删改后同步更新映射。
- `docs/spec/99-context-capsule.md`：阶段变化、上下文压缩或交接前同步更新。

## 5. 当前实施门

当前状态是“用户已明确批准实现，`dxsm-math` v1 已完成本地实现与验收”。实施门已于 2026-08-25 打开。

在用户明确表达“批准方案”“开始实现”或含义等价的授权前：

- 允许读取、审计、修正规范和活文档；
- 允许进行不改变产品实现的只读检查；
- 禁止创建 `skills/dxsm-math/`；
- 禁止创建实现代码、测试代码、README 安装成品或 LICENSE；
- 禁止自动安装、提交或推送。

用户只是要求解释、评审或修改规范时，不视为实现授权。

本项目已经满足上述授权条件。当前可以在 v1 数学范围内创建、修复和验证 Skill、测试与交付文档；用户已明确批准本次 `v1.0.0` 的提交、推送、tag 与 GitHub Release。自动安装以及英语或 408 实现仍未获授权。

## 6. 通用允许清单

- 读取仓库文件和与任务直接相关的本地资料。
- 检查 Git 状态、目录结构、链接、规范编号和一致性。
- 在用户授权范围内修改规范、文档、Skill 和测试。
- 使用本地现有工具做结构检查、数学验证、PDF 读取或图示生成。
- 为复杂任务维护计划，并在阶段结束后更新进度。
- 对输入不足、授权不足或规范冲突提出一个必要的澄清问题。
- 在不改变外部状态的前提下进行诊断与验证。

## 7. 通用禁止清单

- 忽略本文件、`docs/project/lessons.md` 或相关 `docs/SPEC.md` 规范直接开工。
- 把现有 `AGENTS.md` 降级为历史材料或排除在规范来源之外。
- 覆盖用户已有修改、修改无关文件或清理未确认的未跟踪文件。
- 执行未经授权的破坏性命令。
- 使用旧仓库路径写文件。
- 在没有依据时编造题干、符号、条件、定理、计算、答案、来源或测试结果。
- 未经授权扩大任务范围、创建额外产品或改变交付方式。
- 把尚未完成、尚未验证或尚未发布的事项描述为已完成。

## 8. 标准工作流程

1. **开工检查**：完成第 2 节的强制读取和状态检查。
2. **范围确认**：列出本次任务允许修改的对象、明确禁止动作和完成条件。
3. **规范追踪**：定位适用规范编号；没有编号的新产品要求先补规范。
4. **执行修改**：保持最小范围，不碰无关文件。
5. **分层验证**：先验证直接改动，再验证相关规范、测试或链接，最后检查 Git 状态。
6. **纠错同步**：若发生纠正，立即更新 `docs/project/lessons.md`；形成长期规则时更新本文件。
7. **进度同步**：只把实际完成且已验证的事项写入 `[Done]`。
8. **交付说明**：以结果为先，列出关键文件、验证证据、剩余门槛和未执行的外部动作。

## 9. 用户纠正处理协议

收到用户纠正时必须：

1. 先明确承认被纠正的具体事实，不做模糊化处理。
2. 搜索整个仓库中所有受影响表述，不只修正用户指出的一处。
3. 修改直接错误及由它派生的上下文、追踪和进度信息。
4. 在 `docs/project/lessons.md` 记录真实原因与可执行防止规则。
5. 重新运行相关一致性检查。
6. 告知用户改了什么、验证了什么以及是否还有残留。

## 10. 完成定义

一次仓库任务只有同时满足以下条件才算完成：

- 用户要求的交付物已经实际存在；
- 所有适用规范均已满足；
- 必要验证已经实际运行并通过；
- 没有把用户文件或无关改动覆盖掉；
- 活文档已经按规则同步；
- 未经授权的安装、提交、推送、发布或外部调用没有发生；
- 最终说明与工作区真实状态一致。
