# L7：压缩上下文胶囊

> 用途：上下文压缩、任务交接或新 Agent 恢复工作时，先读本文件，再按需读 `docs/SPEC.md` 指向的详细规范。

## 1. 当前目标

在 `/Users/lyn-77/CODE/DaXueShenMai` 中创建三个独立的中国考研 Agent Skills：

- `dxsm-math`
- `dxsm-english`
- `dxsm-408`

v1 只实现 `dxsm-math`。用户已明确打开实施门；当前已创建 Skill、根交付文档和测试资产，结构校验、24 个合成用例、36 道固定真题回归与 12 道未见题均已通过。`v1.0.1` 维护版已使用用户批准且删除“验证”板块后的 Release Notes 完成提交、push、annotated tag 与 GitHub Release。

## 2. 当前状态

- 需求理解置信度：95% 以上。
- 用户于 2026-08-25 明确要求“完整阅读全部文档，阅读结束后开工”，实施门已经打开。
- 完整、分层、分职责规范已经生成，并统一整理到 `docs/`。
- 根目录保留 `AGENTS.md` 作为自动发现的执行入口；项目活文档位于 `docs/project/`。
- 已创建根 `README.md`、MIT `LICENSE`、`skills/dxsm-math/SKILL.md` 和三份 references。
- 已创建 24 个原创合成用例、36 个固定真题回归索引和 12 个封存未见题索引。
- 已完成结构与静态校验并记录 `evals/dxsm-math/results/static-validation.md`；修复了 YAML 流式数组中的未引用矩阵文本和 reference 二级引用链。
- 已执行 24 个原创合成行为用例并记录 `evals/dxsm-math/results/synthetic-report.md`：24/24 通过，阻断失败 0，非阻断缺陷 0。生成与审查均由当前 Codex 会话完成，不是独立 Agent 盲测。
- 已执行 36 道固定真题回归并记录 `evals/dxsm-math/results/fixed-regression-report.md`：数一、数二、数三各 12/12 通过，阻断失败 0，非阻断缺陷 0。
- 固定回归通过后已依 TST-HOLD-003 解封 12 道未见题；每题均先完成原页核验与独立推导，再读取答案册复核并记录 `evals/dxsm-math/results/holdout-report.md`。数一、数二、数三各 4/4 通过，阻断失败 0，非阻断缺陷 0。
- 已依据未见题原页把占位的宽泛题型标签修正为实际知识点；该修正只作用于索引，没有用于改写或调优 Skill。
- 固定回归过程中修正了 1987 旧卷的不歧义题号和页码定位；对没有文本层的扫描页使用原页渲染核验。数一 1987 第九题答案文本提取丢失负号时采用独立推导与回代结论，没有修改源 PDF。
- `skill-creator` 快速校验器使用系统与工作区 Python 时均因缺少 `PyYAML` 退出；未安装任何依赖。Ruby 标准库等价检查已通过，但官方脚本本身不能记为通过；`skills-ref` 当前不可用。
- 最终一致性校验已通过：三份 YAML 可完整解析，24/36/12 用例共 72 个 ID 唯一，固定与未见题报告覆盖完整，Markdown 链接、Skill 结构与一层引用、文本卫生和资源边界均有效。
- `origin`：`https://github.com/Lyn-77/DaXueShenMai.git`。
- 主分支：`main`。
- 根 `.gitignore` 忽略 `.obsidian/`；Obsidian 本地状态不进入版本控制。
- Skill 未自动安装；本次 `v1.0.0` 的提交、推送、tag 与 Release 已获用户明确授权，外部发布结果以 GitHub 为准。
- 已完成逐步任务检查点与上下文压缩保护协议：`docs/project/progress.md` 提供 `[Checkpoint]`，`AGENTS.md`、`docs/SPEC.md` 与本胶囊规则一致；`git diff --check`、24 个 Markdown 文件本地链接、规则关键词和 Git 差异检查均已通过。
- `v1.0.1` 发布提交为 `d164458`；`origin/main`、annotated tag `v1.0.1` 与 GitHub Release 已核验，Release 不是 draft 或 prerelease，正文没有“验证”板块。Release URL：`https://github.com/Lyn-77/DaXueShenMai/releases/tag/v1.0.1`。

## 3. 产品一句话

`dxsm-math` 在用户发送考研数学题、用户错解或定理原理追问时自动触发，通过题干线索定位题型，给出理解型、严格、可追溯的逐步讲解与纠错；纯定义、备考管理和超纲题不处理。

## 4. 核心运行规则

1. 清晰题干也要规范化复述。
2. 模糊或残缺题干先列候选并让用户确认。
3. 输出“学科 → 章节 → 题型 → 方法”。
4. 输出“题干线索 → 数学含义 → 对解法影响”。
5. 每个关键步骤讲清当前状态、原因、公式/定理、运算和新状态。
6. 两种自然方法都存在时完整讲两种，再说明答题者会选哪种及原因。
7. 只有一种有价值的方法时只讲一种。
8. 用户要求纠错但未给过程时先请求过程。
9. 沿用户过程定位首次错误或卡点，从那里继续；原方法非法或明显低效则切换并解释。
10. 不估分、不猜得分点。
11. 复杂计算可用 Agent 已有本地工具复核；检查工具输入、假设、实现和精度后再采信。
12. 工具与推导冲突时回退首次错误处，不隐藏矛盾。
13. 选择题逐项分析；同类干扰只详解一个并标明同类关系。
14. 最终答案有标题、居中、无方框。
15. 易错点只在真实存在时输出。
16. 多题输入只解第一题，结尾询问是否继续第二题；后续复用原截图。
17. 中文题中文、英文题英文、纯公式默认中文。
18. 图示按需；优先确定性代码绘图，禁止生成式数学图。

## 5. 最高可靠性约束

- 所有结论有题干、定理、推导、反例或验证依据。
- 信息不足就追问，无法验证就说明。
- 禁止编造题干、条件、答案、来源、评分点。
- 答案册不是绝对真理，冲突时独立复核。
- Skill 不使用 API Key、不保存密钥、不直接调用云端模型。
- 零强制依赖，不自动安装工具。

## 6. 知识边界

- 覆盖数一、数二、数三的考研数学并集。
- 以最新官方已验证考纲为准；当前可检查的正式数学大纲快照为中国教育考试网 2022 年页面。
- 教育部 2026 年招生工作管理规定确认数学一、二、三仍是全国统一命题科目，但不提供可逐项比较的数学内容正文。
- 2023—2026 正式数学大纲正文尚未逐项核验，变化状态保持 `unknown`；边界有疑问时披露 2022 截止年份。
- 官方新版正文不可得时沿用上一已验证版本，不使用二手来源静默替换。
- 超纲题只说明边界。

## 7. 本地题源

目录：`/Users/lyn-77/NOTE/考研题库/数学真题/`

- 数学一真题 137 页，答案 926 页；
- 数学二真题 130 页，答案 863 页；
- 数学三真题 148 页，答案 882 页；
- 均标注 1987—2026。

六份 PDF 合计约 981 MB，只用于本地验收，不进入仓库。

## 8. 测试门槛

- 公开原创合成用例：24/24 通过，阻断失败 0，非阻断缺陷 0。
- 固定真题回归：36/36 通过，数一/二/三各 12/12，阻断失败 0，非阻断缺陷 0。
- 未见题：12 题在固定回归通过后解封，数一/二/三各 4；12/12 通过，阻断失败 0，非阻断缺陷 0，且从未用于调优。
- 最终答案、关键推导、条件、无依据生成、输入澄清、错解定位、越界和冲突回退均为零容忍阻断项。
- 修复顺序：失败用例 → 同类用例 → 固定回归 → 必要时未见题。

## 9. 交付规则

- 一个仓库，三个独立 Skill。
- v1 目录：`skills/dxsm-math/`。
- 根 README 使用中文且唯一。
- README 提供两种通用安装：人类复制到 `~/.agents/skills/`；把 GitHub URL 交给 Agent 安装。
- 不提供客户端专用目录。
- MIT License。
- 不自动安装、不自动推送。

## 10. 规范入口

- 仓库执行规范：`AGENTS.md`
- 产品规范总索引：`docs/SPEC.md`
- 治理：`docs/spec/00-governance.md`
- 触发与输入：`docs/spec/01-scope-trigger-input.md`
- 职责：`docs/spec/02-runtime-responsibilities.md`
- 流程：`docs/spec/03-solving-teaching-correction.md`
- 输出：`docs/spec/04-output-contract.md`
- 可靠性：`docs/spec/05-reliability-tools-visuals.md`
- 考纲：`docs/spec/06-syllabus-boundary.md`
- 测试：`docs/spec/07-testing-acceptance.md`
- 安装：`docs/spec/08-packaging-installation.md`
- 追踪：`docs/spec/09-traceability.md`

## 11. 下一步

当前阶段是 `dxsm-math` v1.0.0 本地验收与发布准备完成。恢复后按顺序：

1. 先重新读取 `docs/project/lessons.md`、`AGENTS.md` 和本胶囊，再读取 `docs/SPEC.md`、相关分层规范与 `docs/project/progress.md`；
2. 核对 Git 工作区与 `[Checkpoint]`，保留所有用户已有修改和未跟踪文件，不得依赖胶囊中的历史状态猜测哪些文件仍未跟踪；
3. 不重复执行已经通过的 24 个合成用例、36 道固定回归或 12 道未见题，除非后续修改触发相应回归要求；
4. 若用户要求修改 v1，先定位适用规范编号，并按“失败用例 → 同类用例 → 固定回归 → 必要时新未见题”的顺序验证；
5. `v1.0.1` 已完成发布，该一次性发布授权已经执行完毕；后续版本的提交、push、tag 或 Release 仍需新的明确授权；
6. 英语与 408 属于后续版本，必须先分别完成需求与方案，不得直接生成占位实现。

## 11.1 任务检查点与压缩保护

任何进行中的任务都以 `docs/project/progress.md` 的 `[Checkpoint]` 作为断点续作入口：

1. 每完成一个可独立验证的任务步骤，先把完成事实、证据、修改文件、当前步骤、下一动作、阻断项、外部动作状态和恢复指令写入检查点，再开始下一步。
2. 步骤若改变需求、规范、测试结论或阶段事实，同时更新对应规范、追踪矩阵、`lessons.md`、进度或本胶囊，禁止只改一个入口。
3. 主动压缩、可控压缩、收到压缩提示或可预见自动压缩时，先暂停新工作，同步本胶囊和进度检查点，并记录当前中断位置、Git 状态、未提交文件、验证结果和下一条精确操作。
4. 压缩前必须重新读取已写入的检查点和胶囊，确认落盘成功；写入失败时不得继续压缩。
5. 若平台在无预警情况下自动压缩，恢复后先按 `AGENTS.md` 第 2 节重新读取原始规范，核对检查点、Git diff 和产物，补齐缺失事实后再继续。
6. 聊天摘要和计划工具不能替代磁盘检查点；二者冲突时以原始规范、实际文件与可验证 Git 状态为准。

## 12. 执行入口与规范关系

- `AGENTS.md` 是持续生效的仓库执行规范，不是历史文件；任何 Agent 开工时都必须读取并遵守。
- `docs/SPEC.md` 及分层文档是产品行为与验收规范。
- `docs/project/lessons.md` 保存已发生错误的永久防止规则，必须在每次开工时首先读取。
- `docs/project/progress.md` 保存当前进度事实，不负责创造或替换需求。
- 用户最新明确指令优先；`AGENTS.md` 与 `docs/SPEC.md` 分工互补。两者出现冲突时必须向用户报告并请求裁决，不得静默排除任意一方。

## 13. 新会话恢复清单

新会话必须按以下顺序恢复，不依赖旧聊天记录：

1. 打开仓库 `/Users/lyn-77/CODE/DaXueShenMai`。
2. 读取 `docs/project/lessons.md`。
3. 读取根 `AGENTS.md`。
4. 读取本上下文胶囊。
5. 读取 `docs/SPEC.md`，再按任务读取其链接的分层规范。
6. 读取 `docs/project/progress.md`。
7. 运行 `git status --short --branch`、`git remote -v`，确认分支为 `main`、远程为 `origin`，并检查工作区是否干净。
8. 读取 `docs/project/progress.md` 的 `[Checkpoint]`，核对其修改文件、验证证据和下一动作是否与 Git diff 及实际产物一致；一致后从“下一动作”继续，不重复已完成步骤。

恢复后应得到以下唯一状态：

- 需求访谈与规范已经完成，理解置信度达到 95% 以上，实施门已经打开；
- `README.md`、`LICENSE`、`skills/dxsm-math/` 和 `evals/dxsm-math/` 已创建并纳入 `v1.0.0` 发布目标；
- `SKILL.md` 为 169 行并直接链接三份一层 references；结构、静态与合成行为验收已经通过；
- 合成/固定/未见题资产数量分别为 24/36/12；三份 YAML 可解析，合成 24/24、固定 36/36、未见题 12/12 已通过，未见题状态为 `passed`；
- `evals/dxsm-math/results/holdout-report.md` 已生成，12 个 `expected_result` 均映射到 `report:<ID>`；`dxsm-math` v1.0.0 本地验收完成；
- 当前没有正在执行的本地实现或验收事项；`v1.0.0` 发布后进入 v1 系列维护，英语/408 仍需另行启动需求设计；
- `dxsm-english` 与 `dxsm-408` 均属于后续版本；
- 本地六份真题与答案 PDF 只用于测试，不进入仓库；
- 项目不使用 API Key，不自动安装依赖或 Skill；
- `.obsidian/` 必须保持忽略；
- 当前已知工具限制是 quick validator 缺少 `PyYAML`，不能把两次失败运行记为通过；`skills-ref` 也不在 PATH；固定回归时 SymPy 不可用，未安装依赖，矩阵幂改用纯整数计算复核；
- `codex-kaoyan-skills-research.md` 已删除，不应恢复。

如果 Git 状态与上述内容不一致，以实际文件和 Git 证据为准，先审计差异，不得凭胶囊覆盖工作区。
