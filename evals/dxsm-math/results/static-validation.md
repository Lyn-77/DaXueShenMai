# dxsm-math 静态校验报告

- 执行日期：2026-08-25
- 执行者：当前 Codex 会话
- 对象：`skills/dxsm-math/`、根 README/License、公开与本地测试索引
- 结论：仓库定义的等价结构与静态校验通过；官方 quick validator 受环境缺少 PyYAML 限制，不能记为通过；尚未执行行为与真题验收。

## 结果

| 输入定位 | 观察结果 | 断言 | 证据 | 状态 |
|---|---|---|---|---|
| `skills/dxsm-math/SKILL.md` frontmatter | `name=dxsm-math`，description 107 字符，字段仅含 `name`、`description`、`license` | 名称、目录、字段、格式与未完成占位符符合 quick validator 的公开检查逻辑 | Ruby 标准库复现 `quick_validate.py` 的逐项检查并退出 0 | 通过 |
| Skill 包文件清单 | 仅 `SKILL.md` 和三份 references；没有 Skill 内 README、脚本、PDF 或大文件 | v1 包最小、可移植，未创建英语或 408 实现 | 文件白名单、`find` 和大小检查退出 0 | 通过 |
| `SKILL.md` 大小 | 169 行、3434 个 Unicode 字符 | 低于 500 行及约 5000 token 的渐进式披露建议 | Ruby 行数/字符数检查退出 0 | 通过 |
| Markdown 引用 | 16 个仓库内相对链接全部存在；Skill 直接链接 3 份 reference；reference 间链接为 0 | 引用存在且不形成深层引用链 | 相对路径解析与引用深度检查退出 0 | 通过 |
| 三份 YAML 测试资产 | 可由 Ruby YAML 标准解析器完整加载；用例数为 24/36/12，72 个 ID 唯一 | YAML 合法且数量满足规范 | `YAML.load_file`、schema、数量和唯一性检查退出 0 | 通过 |
| 合成测试结构 | 8 类数量分别满足 4/3/2/4/2/3/3/3；每例均有行为、禁止项、阻断项和质量检查 | 满足公开合成集最低结构 | 逐例字段与枚举检查退出 0 | 通过 |
| 固定回归分配 | 数一/二/三各 12；学科分配为 6/2/3/1、8/3/0/1、6/2/3/1；覆盖 1987、2005、2019、2026 | 满足科目、学科和年代分配 | YAML 分组统计检查退出 0 | 通过 |
| 未见题封存 | 数一/二/三各 4；`sealed-not-run`；禁止用于指令调优；仅在固定回归通过后运行 | 未见题仍封存且边界正确 | policy 与状态字段检查退出 0 | 通过 |
| README 与 License | 根 README 唯一，含项目/范围/依赖/两种安装/验证/MIT；License 为标准 MIT | 文档和安装契约完整 | 必要语义、固定命令、路径和许可证文本审查退出 0 | 通过 |
| 文本卫生与资源边界 | UTF-8 有效，无 CR、控制字符、行尾空白、TODO/FIXME/TBD；Skill 内无本地绝对路径；仓库无真题 PDF | 不依赖维护者路径，不打包本地题源 | 全文件字节审计、路径搜索和 Git 文件清单检查退出 0 | 通过 |
| `.obsidian/` | 由根 `.gitignore` 命中，Git 索引中无文件 | 本地编辑器状态不进入版本控制 | `git check-ignore` 与 `git ls-files` | 通过 |
| 官方 quick validator | 系统 Python 导入 `yaml` 时抛出 `ModuleNotFoundError` | 必须如实记录，不能声称官方脚本通过 | 实际运行 `quick_validate.py skills/dxsm-math` 退出 1 | 环境限制 |
| `skills-ref validate` | 当前 PATH 中没有 `skills-ref` | 仅在可用时运行 | `command -v skills-ref` 无结果 | 不可用 |

## 静态指令映射审查

人工逐段对照 `docs/spec/00`—`08`：

- 触发与输入：覆盖截图、自然语言、LaTeX、纯公式、用户过程、定理原理、负向边界、歧义澄清、多题续答和语言选择。
- 解题与教学：覆盖题目复述、分层定位、线索映射、自然方法选择、逐步推导、条件检查、用户首次错误/卡点定位和定理推导。
- 输出：覆盖自适应栏目、选择题逐项分析、最终答案标题与无方框、主推导不用引用块、无真实内容时省略易错点。
- 可靠性：覆盖证据边界、复杂计算本地复核、工具采信前检查、冲突回退、答案册独立复核、工具缺失降级和禁止生成式数学图。
- 包装与边界：覆盖 Agent-only、零 API Key、零强制依赖、不自动安装、不依赖本地 PDF，以及数一/二/三并集与 2022 官方已验证考纲快照回退。

该审查只证明指令覆盖，不证明实际回答行为；行为结论必须由后续 24 个合成用例与本地真题验收给出。

## 本轮发现并修复

1. 合成用例中的矩阵结果含嵌套方括号但未引用，导致 YAML 解析失败；已加引号并重跑全部 YAML 检查。
2. `syllabus-scope.md` 到 `topic-taxonomy.md` 的内部链接形成二级引用链；已删除重复导航链接并重跑全部 Markdown 链接与深度检查。
